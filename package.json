{
  "name": "knowledge-wiki",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Repository-level tooling (commit message linting) for backend/ and frontend/.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.6.1",
    "@commitlint/config-conventional": "^19.6.0",
    "husky": "^9.1.7"
  }
}
